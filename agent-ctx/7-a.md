# Task 7-a: Contact Modal & Events Section

## Agent: Main Agent

## Work Log

### Research & Planning
- Read `/home/z/my-project/worklog.md` to understand the full project history (6 phases of development)
- Read existing component patterns: `faq.tsx` (dialog pattern, Reveal usage, accordion), `newsletter.tsx` (form pattern, loading/success/error states, fetch API), `globals.css` (design system with corner-brackets, concrete-texture, grain-overlay, section-transition, rule-gold, hover-lift, font utilities)
- Read `dialog.tsx` to confirm `showCloseButton` prop API on DialogContent
- Read `reveal.tsx` to understand Reveal, Stagger, StaggerItem animation components
- Read `press-kit.tsx` and `site-footer.tsx` to identify existing "GET IN TOUCH" and "CONTACT US" links
- Read `page.tsx` to understand section order and component integration
- Confirmed zustand is installed (`"zustand": "^5.0.6"`)

### New Files Created

1. **`/home/z/my-project/src/lib/contact-store.ts`** — Tiny zustand store managing contact modal open/close state, shared across components

2. **`/home/z/my-project/src/components/site/contact-modal.tsx`** — Contact inquiry modal component
   - "use client" component using shadcn/ui Dialog with `showCloseButton={false}`
   - Custom close button (X icon, top-right corner, noir-styled border)
   - Dark noir styling: `bg-charcoal-deep`, `border-paper/10`, `bg-black/80 backdrop-blur-xl` overlay
   - Section heading "SUBMIT INQUIRY" in `font-mono-dossier tracking-label text-gold`
   - Subtext in `font-body text-paper-mute`
   - 4 form fields: Name (text), Email (email), Subject (native select with 4 options), Message (textarea, 4 rows, resize-none)
   - All inputs styled with consistent noir input classes (bg-charcoal, border-paper/15, gold focus states)
   - Submit button: full-width, bg-rust, hover:bg-rust-bright
   - Loading state: Loader2 icon + "SUBMITTING…"
   - Success state: Check icon + "INQUIRY SUBMITTED" in gold, form resets on close
   - Error state: text-rust-bright in font-mono-dossier text-sm
   - Custom toast notification on success (DOM-based, noir-styled, auto-dismiss after 4.5s)
   - Confidential note: "All communications are confidential." in small mono text
   - Exports `ContactTrigger` component for use across the page
   - Uses controlled `open`/`onOpenChange` via zustand store

3. **`/home/z/my-project/src/components/site/events-section.tsx`** — Events/readings section
   - "use client" component with `section-transition` class
   - Section id="events" with `grain-overlay concrete-texture` background
   - Section label: "EVENT LOG" in `font-mono-dossier tracking-label text-gold`
   - Heading: "Where the story continues." with `text-glow-gold` and `clamp(2rem,4vw,3.2rem)`
   - Subtitle in `font-body text-paper-mute text-lg`
   - 4 event cards in responsive grid (1 col → sm:2 col → lg:4 col)
   - Events: Doheny Bookstore (Aug 15), USC Panel (Sep 22), Times Square Lit (Oct 8), LAPD Historical Society (Nov 3)
   - Each card: date badge (MONO uppercase), title (display font, gold hover), location (MapPin icon), description (line-clamp-3)
   - Cards have `corner-brackets` class and hover effects (border-gold/40, bg shift)
   - Stagger entrance animations with Stagger/StaggerItem
   - "FOLLOW FOR UPDATES →" link scrolls to #newsletter
   - Faint "APPEARANCES" backdrop text

4. **`/home/z/my-project/src/app/api/contact/route.ts`** — Contact form API endpoint
   - POST handler with validation (name ≥2 chars, valid email, valid subject, message ≥10 chars)
   - Saves to Prisma ContactInquiry model
   - Consistent error response format matching newsletter API pattern
   - GET handler for endpoint discovery

### Files Modified

1. **`/home/z/my-project/prisma/schema.prisma`** — Added `ContactInquiry` model (name, email, subject, message, createdAt)

2. **`/home/z/my-project/src/app/page.tsx`** — Added `EventsSection` (between BuySection and Newsletter) and `ContactModal` (rendered outside main)

3. **`/home/z/my-project/src/components/site/press-kit.tsx`** — Replaced `#newsletter` anchor with `ContactTrigger` for "GET IN TOUCH" button

4. **`/home/z/my-project/src/components/site/faq.tsx`** — Replaced `#newsletter` anchor with `ContactTrigger` for "CONTACT US" link

5. **`/home/z/my-project/src/components/site/site-footer.tsx`** — Replaced 3 `#newsletter` anchors with `ContactTrigger` for "Press & media", "Speaking requests", "Rights & permissions"

### QA Results
- ESLint: Clean (only pre-existing error in `examples/websocket/frontend.tsx`)
- Dev server: Compiled successfully, page returns 200
- Database: Schema pushed successfully, ContactInquiry table created
- All trigger links (press-kit "GET IN TOUCH", FAQ "CONTACT US", footer 3 inquiry links) now open the contact modal via shared zustand store

## Files Created
- `/home/z/my-project/src/lib/contact-store.ts`
- `/home/z/my-project/src/components/site/contact-modal.tsx`
- `/home/z/my-project/src/components/site/events-section.tsx`
- `/home/z/my-project/src/app/api/contact/route.ts`

## Files Modified
- `/home/z/my-project/prisma/schema.prisma`
- `/home/z/my-project/src/app/page.tsx`
- `/home/z/my-project/src/components/site/press-kit.tsx`
- `/home/z/my-project/src/components/site/faq.tsx`
- `/home/z/my-project/src/components/site/site-footer.tsx`